package com.bundle.waja.create

import android.app.Dialog
import android.content.Context
import com.afollestad.materialdialogs.MaterialDialog

/*class CreateAccountDialog {
    fun build(context: Context, title: String, onRenameDone: (String) -> Unit): Dialog {
        val dialog = MaterialDialog.Builder(context)
                .title("Create account")
                .inputRange(1, 100, R.color.colorRementeGreen)
                .input(null, title, { _, newTitle -> onRenameDone.invoke(newTitle.toString()) })
                .positiveText(context.resources.getString(R.string.goal_edit_dialog_rename_button_positive))
                .negativeText(context.resources.getString(R.string.goal_edit_dialog_rename_button_negative))
                .build()

        return dialog
    }
}*/

